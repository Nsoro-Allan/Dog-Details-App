import React from 'react';

const Header: React.FC = () => {
    return (
        <header>
            <h1 className='header-title'>Dog Details App</h1>   
            {/* Add dark mode toggle here */}
        </header>
    );
};

export default Header;